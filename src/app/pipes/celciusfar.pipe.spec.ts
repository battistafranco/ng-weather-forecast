import { CelciusfarPipe } from './celciusfar.pipe';

describe('CelciusfarPipe', () => {
  it('create an instance', () => {
    const pipe = new CelciusfarPipe();
    expect(pipe).toBeTruthy();
  });
});
